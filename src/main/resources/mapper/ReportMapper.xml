<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yihecode.camera.ai.mapper.ReportMapper">

    <select id="selectAlgorithmRatio" resultType="java.util.Map">
        select algorithm_id, count(1) as cnt
        from tbl_biz_report
        ${ew.customSqlSegment}
    </select>

    <select id="selectCamera" resultType="java.util.Map">
        select camera_id, count(1) as cnt
        from tbl_biz_report
        ${ew.customSqlSegment}
    </select>

    <select id="selectCameraAlgorithm" resultType="java.util.Map">
        select camera_id, algorithm_id, count(1) as cnt
        from tbl_biz_report
        ${ew.customSqlSegment}
    </select>

    <select id="selectAlgorithmStatics" parameterType="map" resultType="java.util.Map">
        select algorithm_id, count(1) as ct
        from tbl_biz_report
        where display = 0
          and type = 0
        <if test="startMills != null">
            and created_mills &gt; #{startMills}
        </if>
        <if test="endMills != null">
            and created_mills &lt; #{endMills}
        </if>
        group by algorithm_id
        order by ct desc
    </select>

</mapper>
