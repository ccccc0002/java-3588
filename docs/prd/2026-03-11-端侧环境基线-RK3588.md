# 端侧环境基线（RK3588）

- 更新时间：2026-03-11
- 用途：二期执行期间的端侧连接与验证基线，防止会话重开后丢失关键上下文。

## 固定信息（可追踪）

1. `host`: `192.168.1.104`
2. `username`: `zql`
3. `workdir`: `/home/zql/ks/java-rk3588`
4. `platform`: `RK3588`

## 敏感信息存放约定

密码和令牌不写入可追踪文档。统一放在本地忽略文件：

1. `state/local/rk3588-edge.local.json`

示例格式：

```json
{
  "host": "192.168.1.104",
  "username": "zql",
  "password": "REPLACE_ME",
  "workdir": "/home/zql/ks/java-rk3588"
}
```

## 快速连通验证命令

```powershell
# Windows local
ssh zql@192.168.1.104
```

```bash
# board
cd /home/zql/ks/java-rk3588
pwd
```
